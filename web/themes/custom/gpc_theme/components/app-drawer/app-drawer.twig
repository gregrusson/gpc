<aside
  id="{{ drawer_id }}"
  class="gpc-app-drawer"
  data-drawer
  data-open="false"
  role="navigation"
  aria-labelledby="{{ drawer_id }}-title"
  aria-hidden="true"
>
  <div class="gpc-app-drawer__panel">
    <div class="gpc-app-drawer__header">
      <h2 class="gpc-app-drawer__title" id="{{ drawer_id }}-title">{{ title|default('Navigation') }}</h2>
      <button
        class="gpc-app-drawer__close"
        type="button"
        data-drawer-close
        data-drawer-target="{{ drawer_id }}"
        aria-label="{{ 'Close navigation'|t }}"
      >
        <span aria-hidden="true">×</span>
      </button>
    </div>

    <div class="gpc-app-drawer__content">
      {{ content }}
    </div>
  </div>
</aside>
